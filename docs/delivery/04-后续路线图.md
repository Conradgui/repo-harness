# 后续路线图

本次交付未处理的项目，按优先级排列。每项含方案与判断依据。

---

## ~~P0 · 修复 CI 的 lint 步骤~~ —— 已完成

实测基线：`origin/main` **198 errors**，交付分支修 lint 前 **171**，现在 **0**。

做法不是锁版本了事，而是在 `pyproject.toml` 显式声明规则集（每个族按理由选取，`E501` 行长刻意排除），并把 ruff 锁到 `>=0.16.0,<0.17.0`。**没有配置的 lint 等于把规则集外包给了版本号**——锁版本只是把问题推迟到下次升级。

过程中暴露出一个真实缺陷（标识符时间戳用本地时间，与 `created_at` 的 UTC 不一致），已一并修复并补 6 个用例。详见交付报告 3.4。

---

## P1 · 拆解 `RepoHarness` god object

**问题**：`RepoHarness` 单个类承担 plan mode、工具画像、checkpoint、记忆、skills、workers、指标、token 估算、压缩、自迭代。这是"改一处牵动全身"的根源。

规模与聚类由 `python scripts/measure.py` 产出（`runtime_methods` / `runtime_class_lines` / `runtime_clusters`）：

| 聚类 | 方法数 | 行数 |
|---|---|---|
| core | 42 | 599 |
| tool-proxy | 21 | 137 |
| prompt | 7 | 125 |
| checkpoint | 5 | 124 |
| memory | 16 | 80 |
| metrics/context | 3 | 41 |
| worker | 2 | 9 |
| plan | 3 | 6 |
| skills | 2 | 4 |

### 已完成：第 1 步 —— 结果字段收敛（`be2e628`）

七个 `last_*` 属性收敛为 `core/memory_outcome.py` 的 `MemoryOutcome` 值对象。

> **勘误**：本文档此前称这七个字段"被 CLI 和 display 读取"，据此把提取判定为"需要架构决策"。**说法是错的**——所有读写点都在 `runtime.py` 内部（写 3 处、读 2 处），没有外部消费者，也就没有那个决策。

### 已完成：第 2 步 —— 迁出记忆子域（`1b1fbb9`）

17 个方法迁入 `core/memory_coordinator.py`，`RepoHarness` 保留 12 个同名转发（`cli.py` 和 `core/engine.py` 直接调用它们）。协作者通过 `persist` / `sync` / `source_context` 三个回调反向依赖 runtime，不持有 runtime 引用。

**实际迁移范围与本文档原计划不同**，记录差异避免后续误读：

| 项 | 原计划 | 实际 |
|---|---|---|
| `update_memory_after_tool` | 迁出 | **未迁**。它是工具生命周期钩子，由 `core/tool_executor.py` 在每次工具调用后触发，不属于记忆审核子域 |
| `extract_durable_promotions` / `reject_durable_reason` / `_reject_durable_review_text` | 未列 | **已迁**，是审核链路的组成部分 |
| `_compact_episodic_notes` / `_safe_compaction_parts` / `_self_iteration_candidate_promotions` | 未列 | **已迁**，是自迭代的内部实现 |

迁移中被全量测试抓到两类错误：机械搬运漏了 5 个模块级常量（`self.X` 扫描看不到自由变量），以及生成的转发丢了 keyword-only 参数（`memory_review_edit` 的 `topic` / `text`）。两者都已按协作者的真实签名重建。

### 已完成：第 3 步 —— 持久化解耦（`af64335`）

`remember_candidate` 与 `invalidate_stale_memory` 迁入协作者。范围比原计划小，差异已记录：`memory_text` 是 prompt 关注点（组合记忆 + todo + 压缩摘要），`_persist_memory` 是回调本身，两者都留在 runtime。

按上一轮 gate 的建议，先补了 `tests/test_memory_coordinator.py` 再动手。

### 关于 tool-proxy：不动

该聚类里有一批 `def tool_search(self, args): return toolkit.tool_search(self, args)` 形式的单行转发（数量见 `measure.py` 的 `tool_forwarders`）。它们提供 `agent.tool_search(...)` 绑定方法 API，且 `tests/test_safety_invariants.py` 验证转发确实发生。删掉省不了多少行，却要连带删掉一个验证真实行为的测试。

### 已完成：`sandbox` 第二决策点收敛

`read_only` 模式现由 `PermissionChecker` 判定，返回 `sandbox_read_only`，工具状态从 `error` 变为 `rejected`。`SandboxRunner` 的 raise 保留，服务于绕过权限层的直接调用。

---

## P1 · 补齐核心工具的测试覆盖

`tool_search`、`write_file` / `patch_file` / `run_shell` 参数校验与 sandbox gate、`MemoryCoordinator` 均已补覆盖。剩余：

| 目标 | 现状 | 建议覆盖 |
|---|---|---|
| `run_shell` 输出处理 | 参数校验已覆盖 | 退出码映射、输出截断落盘 |

> **勘误**：本文档此前把 `sandbox` 的 fail-closed 列为「未验证」并建议补两条测试。**这两条测试在 `origin/main` 上就已存在**——`tests/test_sandbox_runner.py` 的 `test_sandbox_required_rejects_when_backend_unavailable` 和 `test_sandbox_read_only_blocks_run_shell_before_subprocess`。这是虚报剩余工作量，已移除该行。

沿用 `test_search_tool.py` 的模式：**能真实执行的真实执行，依赖外部环境的用 argv / 调用断言替代，不要写会 skip 的测试。**

---

## P2 · 评估 `memory_pack.py` 的去留

**现状**：`memory_pack.py` 加上 `cli.py` 的接线与 `tests/test_memory_pack.py`，合计约 1,800 行。有 `/memory_pack` REPL 入口，功能可用。

**本次为何保留**：初诊时列为「与 `memory.py` 职责重叠」的删除候选，但深入后发现它是独立的、有测试的功能，那个「重叠」判断从未被验证。

删除死代码和删除工作中的功能是两件事。后者是产品决策。

**决策要点**：

- 如果项目定位是「展示完整的 agent 能力」→ 保留，它是可演示的差异化功能
- 如果项目定位是「最小可读的 harness 参考实现」→ 删除，这些行数对理解 harness 没有帮助

**若决定删除**：需同步移除 `cli.py` 的 4 个函数（`_load_memory_pack_api` / `_call_memory_pack_function` / `_run_memory_export` / `run_memory_pack_menu`）、REPL 命令分发、`/help` 文本与文档中的相关段落。

---

## ~~P2 · 清理死资源~~ —— 已完成

`assets/screenshots/` 下 3 个 PNG 已删除。它们此前被一个"README 不得引用它们"的测试保护着；那个测试随散文断言一并移除后，文件就成了纯粹的死重。

---

## P3 · 审计未覆盖的大模块

以下模块规模较大且本次未做深入审计：

| 模块 | 行数 | 关注点 |
|---|---|---|
| `memory.py` | 1,265 | 与 `memory_pack.py` 的实际职责边界；Review Queue 的正确性 |
| `context_manager.py` | 644 | 自动压缩触发条件；压缩后信息损失 |
| `run_shell` 危险命令黑名单 | — | 是否可被变形绕过（管道、变量展开、编码） |

`sandbox.py` 的 fail-closed 已有测试覆盖，不在此列。

---

## 长期：防止再次劣化的三条规矩

这三条来自对 51 个 commit 迭代模式的复盘。那个分支的收尾方式是：

```text
写计划文档 → 实现 → 发现风险 → 删掉实现加 gate → 写文档说明 gate → 加测试断言文档写了 gate
```

每轮都"完成了任务"，但产出是限制而不是能力。

**1 · 安全边界用开关，不删实现。**
最致命的一步是把 `tool_write_file` 的函数体删掉换成 `raise`。正确做法是保留实现、加配置项。删掉的能力找不回来，关掉的能力随时能开。

**2 · 每轮迭代以「能跑通的功能」收尾，不以「新增的文档」收尾。**
如果一轮下来只多了文档和 gate，这轮是负产出。

**3 · 测试只测行为。**
不测行数、不测文档措辞、不测文件存在性。这两类测试是重构的水泥——它们不保护正确性，只增加改动成本。
