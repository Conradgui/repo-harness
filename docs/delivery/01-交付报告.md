# RepoHarness 优化交付报告

交付分支：`release/optimized-v1`（基于 `origin/main`）
交付日期：2026-07-27

---

## 一、交付摘要

> **数字来源**：下表全部由 `python scripts/measure.py [ref]` 产出，`origin/main` 为唯一 before 基线。行数定义为**物理行数**（含空行），文件范围取 `git ls-files`，因此未跟踪的临时文件不计入。不要手写这些数字——本文档初版因为手写而出现过跨口径相减的错误，见第九节。

| 指标 | `origin/main` | `release/optimized-v1` | 变化 |
|---|---|---|---|
| ruff lint | 198 errors | <!-- measure:ruff_errors -->**0** | <!-- delta:ruff_errors -->−198 |
| 测试结果 | 有失败（数量见 3.1 说明） | **全绿 / 0 failed** | — |
| 源码行数 | 17,167 | <!-- measure:source_lines -->**14,794** | <!-- delta:source_lines:pct -->−2,373（−13.8%） |
| 源码文件数 | 79 | <!-- measure:source_files -->76 | <!-- delta:source_files -->−3 |
| 测试行数 | 8,489 | <!-- measure:test_lines -->9,625 | <!-- delta:test_lines -->+1,136 |
| 测试文件数 | 34 | <!-- measure:test_files -->40 | <!-- delta:test_files -->+6 |
| `RepoHarness` 方法数 | 108 | <!-- measure:runtime_methods -->101 | <!-- delta:runtime_methods -->−7 |
| `RepoHarness` 类体行数 | 1,495 | <!-- measure:runtime_class_lines -->1,115 | <!-- delta:runtime_class_lines -->−380 |

> **哪些数字是机器校验的**：上表 `release/optimized-v1` 列中带 `<!-- measure:key -->`
> 标记的 7 个数字由 `scripts/measure.py` 产出，`tests/test_delivery_figures.py`
> 会在它们与脚本不符时让测试变红。**其余数字（包括 before 列与 delta 列）是散文，
> 未被机器校验**——它们由人工在重新测量后填写，仍可能过期。
>
> 测试**数量**不写进本表：`test_docs_integrity.py` 按文档数参数化，加一篇 markdown
> 就多一个用例，当交付指标会自我膨胀。

复现方式：

```bash
python scripts/measure.py origin/main
python scripts/measure.py
```

提交列表（每个都独立可回退）：

```bash
git log --oneline origin/main..HEAD
```

> 提交数不写进文档——它在下一次提交时就会过期。`measure.py` 的 `commits_ahead_of_main` 给出当前值。

---

## 二、决策依据：为什么从 main 重建，而不是修 codex 分支

项目此前在 `codex/repo-harness-phase1-security-kernel` 分支上迭代了 51 个 commit。诊断发现该分支**不可救**，理由是量化的：

### 2.1 分支投入的构成

| 类别 | 新增行数 | 占比 |
|---|---|---|
| 文档 | +6,374 | 41% |
| 测试 | +5,612 | 36% |
| **源码** | **+2,765** | **18%** |

77% 的工作量产出的是文档和测试。（行数取 `git diff --numstat origin/main..codex/...` 下 `docs/`、`tests/`、`repo_harness/` 三个前缀的新增量，三者之和小于总量——差额是根目录文件与配置。）而 18% 的源码里，新增的 `repo_harness/security/`（isolation 268 + authorization 123 + staging 55）本身就是封锁机制。

### 2.2 核心能力被四层封锁，且底层实现已删除

| 层 | 位置 | 行为 |
|---|---|---|
| 1 | `tools/__init__.py:553` | `tool_write_file` / `tool_patch_file` 函数体被删，只剩 `raise` |
| 2 | `core/tool_profiles.py:17` | `ToolSetProfile.__post_init__` 从**所有** profile 剥离写工具 |
| 3 | `permissions.py:35` | 权限检查第一行无条件 deny |
| 4 | `auto_issue_fix/runner.py:167` | `_block_on_phase0` |

实测验证（`scripts/permission_probe.py`，两个分支各跑一次，7 种配置 × 2 个写工具 = 14 个场景）：

| 场景 | main | codex 分支 |
|---|---|---|
| 默认 approval=ask | **allow** | deny |
| approval=auto | **allow** | deny |
| plan mode | deny | deny |
| 允许写入的场景数 | **6 / 14** | **0 / 14** |

结论：该分支上的 agent 无法修改任何文件——它不是 code agent，是只读问答系统。而 `main` 的权限分层完好、写入实现完整。

### 2.3 分支测试是红的

`codex` 分支的测试是红的，其中一部分是该分支自己引入的——最新 commit `2d62a8c` 就引入了 3 个 `ConfigError: state_root_unavailable`。

> 具体失败**数量**不在此断言：suite 计数依赖运行环境，理由见 3.1。可复算的是引入来源——`git show 2d62a8c` 能看到那 3 个。

### 2.4 cherry-pick 不可行（实测结论）

对 51 个 commit 做了完整依赖图分析和 dry-run cherry-pick 实测：

- 分支底座 `8eaf1d5` 是一个 **57 文件 / +4,461 行**的 commit，重写了 `tests/conftest.py`。所有后续测试类 commit 都依赖它。
- 保留它 = 一次性吞下 4,461 行测试基建，且它是整条依赖链的根。
- 丢弃它 = 后续 commit 全部无法 cherry-pick，只能手工移植。

因此采用**手工移植**：把真正有价值的修复（rg 参数注入）以补丁形式打到 main 上，而不是 cherry-pick。

---

## 三、修复的四类真实缺陷

### 3.1 子进程输出编码（影响 37 处调用点）

**症状**：中文 Windows 上，凡是读取子进程输出的代码路径都可能拿到空输出；调用方在很远的地方报 `TypeError: argument of type 'NoneType' is not iterable`。

> **关于"main 上有几个测试失败"：这个数字已放弃断言。**
>
> 本文先后写过「5 个失败」和「1 个失败」，两次都基于单次测量。第三次用隔离
> worktree 复测得到 5 failed，第四次由独立审核用全新 venv 在三种路径下复测得到
> **0 failed**。两次谨慎的测量结论相反，说明**这个数字依赖运行环境**（checkout 路径、
> locale、venv 布局），不适合作为论据。
>
> 缺陷本身与这个数字无关，且可直接复现，不依赖测试套件：
>
> <!-- verify:python -->
> ```python
> import subprocess
> import sys
>
> code = "import sys; sys.stdout.buffer.write('中文'.encode('utf-8'))"
>
> without = subprocess.run([sys.executable, "-c", code], capture_output=True, text=True)
> with_utf8 = subprocess.run(
>     [sys.executable, "-c", code],
>     capture_output=True, text=True, encoding="utf-8", errors="replace",
> )
>
> # 指定 encoding 后输出完好；不指定时，在 cp936 主机上它会丢失。
> assert with_utf8.stdout == "中文"
> assert without.stdout != with_utf8.stdout or with_utf8.stdout.isascii()
> ```
>
> 在 cp936（中文 Windows）主机上，这会让 subprocess 的读取线程抛
> `UnicodeDecodeError: 'gbk' codec can't decode`，而**该异常不传播**——调用方拿到的是
> 空输出。加上 `encoding="utf-8"` 后不再发生。
>
> 另一条可直接核实的事实：修复前有 12 处源码调用点缺 `encoding=`。
>
> **教训**：一个依赖环境的数字，无论测多少次都不该当作论据。而用未经严格验证的测量去
> "修正"一个结论——本文干过一次——比原来的错误更危险，因为它带着"已核实"的权威。

**根因**：所有 `subprocess.run(..., text=True)` 都没有指定 `encoding=`。Python 使用系统 locale 编码（中文 Windows 是 GBK）解码子进程输出，遇到 UTF-8 字节抛 `UnicodeDecodeError`。

**为什么长期未被发现**：解码错误发生在 subprocess 的**读取线程**里。线程崩溃后 `.stdout` 变成 `None`，调用方才在使用时报错。错误信息完全指不到真正的原因。

**影响范围远超测试**：`run_shell`、`search` 的 rg 路径、sandbox runner 都在此列。agent 执行任何输出含非 ASCII 的命令，拿到的都是 `None`。

**修复**：12 处源码 + 25 处测试统一加 `encoding="utf-8", errors="replace"`。`errors="replace"` 保证真正的非 UTF-8 输出（二进制、混合编码）不会再次让读取线程崩溃。

### 3.2 搜索工具双后端语义不一致 + 两个安全缺口

**症状**：同一次搜索，装没装 `ripgrep` 结果不同。

**根因**：
- rg 路径把 pattern 当**正则**
- Python fallback 是 `pattern.lower() in line.lower()`，当**字面量**且总是忽略大小写

**附带的两个安全缺口**：
- 缺 `--` 终止符：以 `-` 开头的 pattern 被 rg 当作选项。模型传入 `--files` 会让工具列文件名而非搜索——模型可以通过 pattern 参数操纵工具行为。
- 缺 `--fixed-strings`：模型提供的 pattern 被编译成正则，可触发灾难性回溯（ReDoS）。

**修复**：统一为字面匹配契约。rg 加 `--fixed-strings` 和 `--`，smart-case 判断（仅当 pattern 含大写时区分大小写）提取为两条路径共用的单一表达式。

### 3.3 零测试覆盖的核心工具

`tool_search` 此前**完全没有测试**。新增 `tests/test_search_tool.py`，13 个用例。

设计要点：rg 后端通过**断言 argv** 来验证，而不是要求机器上装了 ripgrep。这样开发机和 CI 行为一致——**会 skip 的测试等于没测**。

已验证测试是有效的：撤销源码修复后，13 个中有 4 个立即变红。

---

### 3.4 标识符时间戳与 `created_at` 用了两套时钟

**症状**：无报错。这是一个只在跨时区比对或 DST 切换时才会显形的问题。

**根因**：session / task / run 的 id 用 `datetime.now()`（本地时间）生成，而写在**同一个 dict 字面量里**的 `created_at` 用的是 UTC：

```python
"id": datetime.now().strftime("%Y%m%d-%H%M%S") + "-" + uuid.uuid4().hex[:6],  # 本地
"created_at": now(),                                                          # UTC
```

**为什么是缺陷**：

- 本地时间不单调。DST 回拨会重复一小时，两次相隔一小时的运行可能产生**排序颠倒**的 id
- id 与自己的 `created_at` 相差一个本地时区偏移，排障时对不上
- 运行工件跨机器比对时基准不一致

**修复**：四个调用点统一走 `workspace.id_timestamp()`。新增 `tests/test_identifiers.py` 六个用例钉住 UTC 契约，其中一个专门断言 id 与它自己的 `created_at` 一致。

已验证测试有效：把 helper 改回本地时间，6 个中有 5 个立即变红（本机 UTC+8）。

---

## 四、拆除的反重构机制

### 4.1 最小行数棘轮

`tests/test_architecture_boundaries.py` 整个文件只有一个测试，对 10 个模块强制**最小行数**：

```python
minimum_lines = {"repo_harness/core/tool_executor.py": 120, ...}
assert len(lines) >= minimum, f"{relative} still looks like a shim"
```

**代码变短就失败。** 这是项目只增不减的直接机制——任何精简都要先和测试套件打架。

它本身是上一轮重构翻车的疤：模块被拆到 `core/` 和 `features/`，顶层留下 shim（`worker_manager.py` 5 行、`skills.py` 12 行），有人加了这个测试防止真模块再被掏空。

### 4.2 文档散文断言

`test_repo_harness.py` 中 11 个函数断言特定句子出现在特定 markdown 文件里，包括中文散文：

```python
assert "文档同步是功能完成后的必需门禁" in maintainer_readme
```

这类测试**焊死措辞而不保护正确性**：任何改写都会失败，而链接指向已删除文件时它照样通过。

### 4.3 替换方案

删除 309 行字符串匹配，新增 `tests/test_docs_integrity.py`（按文档数参数化，数量随文档增长），验证真正重要的东西：

- 全部内部 markdown 链接可解析
- README 指向的 getting-started 指南存在
- 指南让读者运行的 CLI 入口在打包配置里已声明
- 没有空的 markdown 文件

---

## 五、删除的死重

| 模块 | 行数 | 依赖情况 |
|---|---|---|
| `metrics.py` | 1,712 | **零依赖**，只有自己的测试引用 |
| `evaluator.py` | 651（该提交删除时的行数） | 仅被 `metrics.py` 依赖 |
| `release_evidence.py` | 298 | 源码中零引用 |
| 4 个实验脚本 + benchmark 数据 + 3 个测试文件 | 其余 | 仅被上述模块使用 |

该提交实测共删 3,401 行（含随之删除的三个测试文件），**唯一消费者是它们自己的测试覆盖**。而 agent 控制循环（`core/engine.py`）只有 448 行。

保留这些意味着每次改 runtime 都要兼容一套没人运行的评测框架。

---

## 六、明确保留的部分（及理由）

| 模块 | 行数 | 保留理由 |
|---|---|---|
| `memory_pack.py` | 1,092 | **活功能**，有测试、有 CLI 入口（`/memory_pack`）。删它是产品决策而非清理 |
| `runtime_evidence.py` | 62 | 接在 run 生命周期里，`runtime.py` 实际调用 |
| `evaluation/run_evidence.py` | 319 | 与已删的 `release_evidence.py` 是不同模块，有测试覆盖 |
| brand marker 守卫 | — | 检查的是真实可能回归的东西，不是措辞 |

**关于 `memory_pack.py`**：初诊时我把它列为"与 memory.py 职责重叠"的删除候选，但没有验证这个重叠。深入后确认它是独立的、可用的、有测试的功能。删除工作中的功能和删除死代码是两件事。列为**可选精简项**，由产品负责人决定。

---

## 七、已识别但未处理的问题

诚实标注，避免后续踩坑：

### 7.1 `RepoHarness` god object —— 已推进两步

`RepoHarness` 单个类承担 plan mode、工具画像、checkpoint、记忆、skills、workers、指标、token 估算、压缩、自迭代。这是"改一处牵动全身"的根源。

**已完成**：

- 第 1 步（`be2e628`）：7 个散落的 `last_*` 结果字段收敛为 `MemoryOutcome` 值对象
- 第 2 步（`1b1fbb9`）：记忆子域 17 个方法迁入 `core/memory_coordinator.py`，保留 12 个同名转发维持 API。协作者通过三个回调反向依赖 runtime，不持有 runtime 引用

实时规模跑 `python scripts/measure.py`（`runtime_methods` / `runtime_class_lines`）。

> **勘误**：本文初版称记忆聚类的 7 个 `last_*` 字段"被 CLI 和 display 读取"，并据此把提取判定为"需要架构决策"。**说法是错的**——所有读写点都在 `runtime.py` 内部。那个决策不存在，两步都是直接可做的。

- 第 3 步（`af64335`）：`remember_candidate` 与 `invalidate_stale_memory` 迁入协作者。范围比原计划小——`memory_text` 是 prompt 关注点、`_persist_memory` 是回调本身，两者留在 runtime

三步全部完成。协作者层已补 32 个用例（`tests/test_memory_coordinator.py`）。

### 7.2 CI 的 Lint 步骤 —— 已修复

> **勘误**：本文初版称 lint 错误数为 199，且「改动前后均为 199，零新增」。这两个数字都未经实测。实际核验结果见下。

仓库原先**没有任何 ruff 配置**，`ruff check .` 用的是所装版本恰好携带的默认规则集。而 `ruff>=0.4.4` 没有上限，会解析到 0.16.0，于是 CI 一直在为没人选择过的规则失败。

实测基线（用 git worktree 隔离对比）：

| 分支 | ruff 错误数 |
|---|---|
| `origin/main` | **198** |
| 交付分支（修 lint 前） | **171** |
| 交付分支（修 lint 后，终态） | **0** |

> 注：上表为修 lint 过程中的中间态记录；最终交付分支 `ruff check .` 错误数为 **0**（与摘要 measure 标记 `ruff_errors=0`、changelog v7 一致）。

已在 `pyproject.toml` 显式声明规则集并把 ruff 锁到 `>=0.16.0,<0.17.0`。规则按理由逐条选取而非用前缀扩大范围，`E501`（行长）刻意排除。

修这 198 个问题时暴露出一个**真实缺陷**，见 3.4。

### 7.3 `assets/screenshots/` —— 已删除

3 个 PNG 曾被提交，但有测试强制 README **不得**引用它们。该测试已随散文断言一并删除后，这些文件成了无人引用的死文件，现已移除。

### 7.4 后续审计轮次已覆盖的模块

以下模块规模较大，已在后续审计轮次（见 `04-后续路线图.md` P3 与 `project-review-report.html`）完成深入审计：

- `memory.py`（1,266 行）：修复 `accept_durable_review` 部分提交 bug，确认 `skip_durable_review` 不改状态为设计意图，压缩逻辑迁移至 `core/compact.py`。
- `context_manager.py`（668 行）：修复 5 个问题（metadata `section_order` 缺 skills、`floor` 可大于 `budget`、`note["text"]` 直接访问可能 `KeyError`、`compute_budgets` 极小 context_window 时 `output_reserve` 超容量、CLI 异常消息缺失异常类型名）。

`sandbox.py` 的豁免机制本轮审计出三个逃逸，最终结论是**该方案不成立**：`read_only` 下已取消豁免。详见 [ADR-007](../decisions/007-read-only-不再有豁免.md)。

## 八、核心能力验证

本次交付最重要的一条结论——**agent 具备真实的文件写入能力**，有三条独立证据：

1. **权限层实测**：`main` 的 `PermissionChecker` 在默认和 auto 策略下对 `write_file` 返回 allow
2. **实现完整**：`tool_write_file` 在 main 上是完整实现（`path.write_text(...)`），非 stub
3. **端到端测试**：`test_public_cli_uses_mock_openai_provider_without_live_key` 驱动完整 CLI，断言 agent 真实写出了 `mock.txt` 且内容正确——该测试在交付分支上通过

---

## 九、如何验证本次交付

```bash
git checkout release/optimized-v1
uv sync
uv run pytest tests/ -q
```

预期：`All checks passed`，且测试全绿无失败。

逐个提交回退验证：

```bash
git log --oneline origin/main..HEAD
git show <sha>
```

每个提交都是自包含的，可单独 revert 而不影响其他。
